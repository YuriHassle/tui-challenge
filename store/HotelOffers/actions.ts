import { Actions } from './types';

export default {
  async fetchOffersByHotel(commit, payload) {
    //const app = window.$nuxt.context.app;
    //const { data: offers } = await app.$hotelsRepo.getOffersByHotel(payload);
    //context.commit('SET_OFFERS', offers);
  },
} as Actions;
